{
  "_comment": "Dependency pins for the Layer-C assistant-ui example (verified June 2026). assistant-ui moves fast — run `npm view @assistant-ui/react version` and re-verify the runtime API against the docs before relying on these. Scaffold the rest with `npm create vite@latest -- --template react-ts`, then merge these deps.",
  "dependencies": {
    "@assistant-ui/react": "0.14.23",
    "@open-agent-loops/core": "^0.0.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}

# scripts/.deploy.env — sourced by scripts/ship.sh. Copy to scripts/.deploy.env
# and fill in. GITIGNORED; NEVER commit real tokens.

# npm AUTOMATION token (npmjs.com → Access Tokens → Classic → "Automation").
# Must be the Automation type — it bypasses the 2FA-on-publish requirement.
NPM_TOKEN=npm_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Cloudflare creds for the docs deploy. EITHER set both directly here…
# CLOUDFLARE_API_TOKEN=cf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# CLOUDFLARE_ACCOUNT_ID=11a1edfd3dc0624feacbfa1db3a6760a   # Recursal PROD (owns featherless.ai)

# …OR point at an existing deploy-env file that already defines them:
DOCS_DEPLOY_ENV=/Users/Crossproduct/Desktop/featherless/guarded-api-cloudflare/worker/.deploy.env

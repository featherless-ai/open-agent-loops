---
title: agent-core
description: A minimal, provider-agnostic agentic loop built on swappable interfaces.
template: splash
hero:
  tagline: A minimal, provider-agnostic agentic loop. Every fundamental piece sits behind an interface, so it swaps without touching the loop.
  actions:
    - text: Getting started
      link: /getting-started/
      icon: right-arrow
      variant: primary
    - text: API reference
      link: /api/
      icon: open-book
      variant: minimal
---

import { Card, CardGrid } from "@astrojs/starlight/components";

<CardGrid stagger>
  <Card title="Plug-and-play seams" icon="puzzle">
    The model client, memory, tools, and stop conditions are all interfaces.
    Implement them with a plain object or function — never subclass.
  </Card>
  <Card title="Streaming by default" icon="rocket">
    `ModelClient.stream()` returns an async iterable of `StreamEvent`s, so
    reasoning, text, and tool calls arrive incrementally.
  </Card>
  <Card title="Provider-agnostic" icon="random">
    Nothing in the core references a specific LLM SDK. Any OpenAI-compatible
    endpoint works with a raw `fetch` — no extra package required.
  </Card>
  <Card title="Independently testable" icon="approve-check">
    Each seam is verified in isolation with deterministic test doubles —
    zero network, fully reproducible.
  </Card>
</CardGrid>
